import 'package:flutter/material.dart';

class EditKlasemen extends StatelessWidget {
  const EditKlasemen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: const Color(0xff142D4C),
        title: const Text('Edit Klasemen (belom jadi)'),
      ),
    );
  }
}
