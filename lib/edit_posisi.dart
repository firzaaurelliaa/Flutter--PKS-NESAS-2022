import 'package:flutter/material.dart';

class EditPosisiAKL extends StatelessWidget {
  const EditPosisiAKL({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: const Color(0xff142D4C),
        title: const Text('Edit Posisi (belom jadi)'),
        elevation: 0,
      ),
    );
  }
}
