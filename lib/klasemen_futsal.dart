import 'package:flutter/material.dart';

class KlasemenFutsal extends StatelessWidget {
  const KlasemenFutsal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xff142D4C),
        title: Text('Klasemen Futsal'),
      ),
    );
  }
}
