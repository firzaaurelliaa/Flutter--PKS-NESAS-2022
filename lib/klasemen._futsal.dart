import 'package:flutter/material.dart';

class KlasemenFutsal extends StatelessWidget {
  const KlasemenFutsal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Klasemen Futsal'),
      ),
    );
  }
}
