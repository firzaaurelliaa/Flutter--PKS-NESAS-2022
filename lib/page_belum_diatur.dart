import 'package:flutter/material.dart';


class PageBelumDiatur extends StatelessWidget {
  const PageBelumDiatur({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Page belum diatur'),),
    );
  }
}